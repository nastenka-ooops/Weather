<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Базовый стиль для кнопок выбора -->
    <style name="FlatChoiceButton" parent="Widget.AppCompat.CompoundButton.RadioButton">
        <item name="android:button">@null</item>
        <item name="android:background">@drawable/radio_button_selector</item>
        <item name="android:layout_weight">1</item>
        <item name="android:gravity">center</item>
        <item name="android:padding">12dp</item>
        <item name="android:textColor">@drawable/radio_text_selector</item>
        <item name="android:fontFamily">@font/poppins_black</item>
        <item name="android:layout_margin">4dp</item>
    </style>
</resources>