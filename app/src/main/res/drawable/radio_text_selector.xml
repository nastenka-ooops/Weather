<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:color="@color/white" android:state_checked="true" />  <!-- Выбранный -->
    <item android:color="@color/dark_gray" android:state_checked="false" /> <!-- Невыбранный -->
</selector>